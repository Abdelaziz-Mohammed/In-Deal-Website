export const metadata = {
  title: "In-Deal Admin",
  description: "In-Deal Admin Panel",
};

export default function AdminRootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
